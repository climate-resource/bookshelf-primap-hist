Replaced the tests that targeted the retired `bookshelf-producer` with tests that load the recipe,
check every book resolves to the expected merged metadata,
and record and validate every version through the CLI.
