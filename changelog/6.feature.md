Derive the process activity id and the resource tracking ids from `uuid5` over the repository URL and the version.
Re-recording an unchanged build now produces the same bundle hash instead of minting fresh identifiers each run.
