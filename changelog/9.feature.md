Declared every upstream release in `books:`, so `bookshelf record --version` picks one.
v2.6, v2.6.1 and v2.7 each carry their own licence, DOI, release date and the Zenodo
`uri:` and `sha256:` the raw CSV is fetched and verified against.
