Pulls in the copier template updates from `copier-bookshelf-dataset` main.
This replaces the hand-written `scripts/feedstock.py` with the `bookshelf record`, `validate` and `publish` CLI,
splits CI into `feedstock-ci` and `feedstock-publish`, and moves the book visibility declaration into `bookshelf.yaml`.
