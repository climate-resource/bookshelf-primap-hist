Moves the recipe to the sectioned `volume:`, `defaults:` and `books:` shape.
The three published versions are declared once in `bookshelf.yaml`, each with its upstream DOI,
release date, licence and raw Zenodo CSV, and `bookshelf record --version` picks one.
The per version input table, the hand rolled fetch and the `uuid5` identifiers are gone from `build.py`,
which now holds only the processing.
