Rebuilt the feedstock from `copier-bookshelf-dataset` v0.3.0b1.
The `bookshelf-producer` notebook and its `src/primap-hist.yaml` config are gone,
replaced by a `bookshelf.yaml` recipe and a `build.py` that record into a bundle.
`make run VERSION=v2.7` records and validates a book, and `make publish` replays it to the API.
