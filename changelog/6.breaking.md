Move the feedstock onto the platform record and replay flow.
The `src/primap-hist.py` notebook and its paired metadata are replaced by a standalone `build.py`,
and the collection, licence, visibility and authors are now declared in `bookshelf.yaml`.
The raw Zenodo CSV is catalogued as an external pointer rather than re-hosted.
